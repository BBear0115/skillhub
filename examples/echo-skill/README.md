Zip the contents of this folder, keeping `skill.json` at the root of the archive, then upload it from the SkillHub UI.

Expected behavior:
- Upload registers one tool named `echo`
- The online tester can call it with `{"text":"hello"}`
- The tool returns `echo:hello`
